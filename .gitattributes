# Keep LF in the working tree on every OS (Windows core.autocrlf=true would otherwise
# check files out as CRLF, which breaks Prettier's check and shell scripts run in containers).
* text=auto eol=lf
